# Flutter plugins include their own consumer rules. Keep annotations and generic
# signatures because several plugins discover platform methods reflectively.
-keepattributes RuntimeVisibleAnnotations,RuntimeInvisibleAnnotations,Signature
